window.addEventListener('DOMContentLoaded', (event) => {
    var removeAlert = document.getElementById('alertForRemoving');
    if (removeAlert) {
    	alert("The following items that are highlighted red are currently sold out. Please manually remove those items.");
    }
});